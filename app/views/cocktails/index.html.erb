<strong>All the cocktails</strong>

<% @cocktails.each do |c| %>
<div>
  <p><%= link_to c.name, cocktail_path(c) %></p>
</div>
<% end %>

<%= link_to "Create a cocktail", new_cocktail_path, class: "btn btn-primary" %>
